name: Update Composer

on:
  push:
    branches:
      - develop-aws

jobs:
  update-composer:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Remove composer.lock and vendor directory
      run: |
        rm -rf vendor composer.lock

    - name: Install Composer
      run: |
        curl -sS https://getcomposer.org/installer | php
        sudo mv composer.phar /usr/local/bin/composer

    - name: Install Dependencies
      run: |
        composer config platform.php 7.4
        composer install --ignore-platform-reqs

    - name: Commit and Push Changes
      run: |
        git config --global user.email "htetlinoo@qualymm.com"
        git config --global user.name "QMHLO"
        git add composer.lock
        git commit -m "Remove vendor and composer.lock, re-install dependencies"
        git push
      env:
        GITHUB_TOKEN: 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
